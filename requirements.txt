# ---- audio I/O ----
sounddevice==0.5.5
numpy==2.5.2
scipy==1.18.0

# ---- voice activity detection ----
silero-vad==6.2.1

# ---- speech to text ----
faster-whisper==1.2.1

# ---- japanese morphology & romaji ----
janome==0.5.0
pykakasi==2.3.0
# Optional second tokenizer, for --tokenizer unidic. Left out of the default
# install because the dictionary is a separate ~1.2 GB download that pip does
# not carry, so requiring the wheel here would promise something it cannot
# deliver. To enable:
#     pip install fugashi==1.5.2
#     curl -LO https://clrd.ninjal.ac.jp/unidic_archive/2512/unidic-cwj-202512.zip
#     unzip -d ~/.local/share/tsutawaru/unidic-cwj-202512 unidic-cwj-202512.zip
# Then `python run.py --tokenizer unidic`, or set [nlp] tokenizer/unidic_dir.
# fugashi==1.5.2

# ---- translation ----
deep-translator==1.11.4
httpx==0.28.1

# ---- ui / cli ----
rich==15.0.0
typer==0.27.1

# ---- infra ----
platformdirs==4.11.3
tenacity==9.1.4
pytest==9.1.1
